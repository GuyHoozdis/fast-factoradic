# fast-factoradic
O(1) calculations of a permutation in nPr-space via factoradics.

## Development

Install the development dependencies with:

```bash
uv sync --group dev
```

Run the lint and test automation with:

```bash
uv run nox -s lint
uv run nox -s tests
```
