/using-superpowers

Create a repo for a Python project.
- Use `uv` for project management.
- Use `nox` for development automation.
- Use `ruff` for linting and formatting.
  - Create a strict `ruff.toml` configuration.
- Use `hypothesis` for testing.
- Use `pytest` for running tests.
- Use `src` layout for the project structure.

